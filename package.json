{
  "name": "wiretap",
  "description": "> _\"While our competitors are deploying today's strategies, we're already > frontrunning tomorrow's mistakes.\"_ > > — Copyright © Franklin J. Merrimont, Founder & Chairman, The Firm",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "build:packages": "turbo run build --filter=@wiretap/db --filter=@wiretap/utils --filter=@wiretap/config",
    "dev": "turbo run build:packages && turbo run dev",
    "dev:watchers": "pnpm run build:packages &&  turbo run dev --filter=@wiretap/token-created-watcher --filter=@wiretap/config --filter=@wiretap/utils --filter=@wiretap/db",
    "dev:webapp": "pnpm run build:packages && turbo run dev --filter=@wiretap/webapp --filter=@wiretap/config --filter=@wiretap/utils --filter=@wiretap/db",
    "test": "pnpm run test-db:start && pnpm run test-db:migration && turbo run test ; pnpm run test-db:stop",
    "test:watch:watchers": "pnpm run test-db:start && pnpm run test-db:migration && turbo run test:watch --ui=tui --filter=@wiretap/token-created-watcher; pnpm run test-db:stop",
    "test:watch:db": "pnpm run test-db:start && pnpm run test-db:migration && turbo run test:watch --ui=tui --filter=@wiretap/db; pnpm run test-db:stop",
    "lint": "turbo run lint",
    "ts:check": "turbo run ts:check",
    "db:migration": "turbo run db:migration",
    "db:migration-generate": "turbo run db:migration-generate",
    "db:migration-push": "turbo run db:migration-push",
    "test-db:start": "docker compose -f docker-compose.test.yaml up -d",
    "test-db:migration": "turbo run test-db:migration --filter=@wiretap/db",
    "test-db:stop": "docker compose -f docker-compose.test.yaml down"
  },
  "devDependencies": {
    "@total-typescript/tsconfig": "^1.0.4",
    "@types/mime": "3.0.4",
    "@types/node": "^22.14.1",
    "eslint": "^9.24.0",
    "prettier": "3.5.3",
    "tsx": "4.19.3",
    "turbo": "^2.5.0",
    "typescript": "^5.8.3"
  },
  "packageManager": "pnpm@10.8.1",
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "three": "^0.176.0"
  }
}
