export const getDexScreenerUrl = (tokenAddress: string) => {
  return `https://dexscreener.com/base/${tokenAddress}`;
};

export const getClankerWorldUrl = (tokenAddress: string) => {
  return `https://www.clanker.world/clanker/${tokenAddress}`;
};
